import { FC } from "react";
import { StyledLogo } from "./style";

const Logo: FC = () => {
    return (
        <StyledLogo path="/">
            Support<span>Nanny</span>
        </StyledLogo>
    );
};

export default Logo;
